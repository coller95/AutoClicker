name: Build and Release

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags like v1.0.0, v1.0.1, etc.
  workflow_dispatch:  # Allow manual trigger from GitHub Actions UI

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Build executable
      run: python build.py
    
    - name: Verify build
      run: |
        if (Test-Path "dist\AutoClicker.exe") {
          Write-Host "✓ Build successful!"
          $size = (Get-Item "dist\AutoClicker.exe").Length / 1MB
          Write-Host "File size: $([math]::Round($size, 2)) MB"
        } else {
          Write-Error "Build failed - executable not found"
          exit 1
        }
      shell: pwsh
    
    - name: Create Release and Upload Assets
      uses: softprops/action-gh-release@v1
      with:
        name: AutoClicker ${{ github.ref_name }}
        body: |
          ## AutoClicker ${{ github.ref_name }}
          
          ### 📥 Download
          Download `AutoClicker.exe` below and run it - no installation needed!
          
          ### ✨ Features
          - 🎬 Record and replay mouse clicks and keyboard inputs
          - ⚡ **Rapid-fire spam click (F3)** - 100 clicks per second!
          - 🎨 **Visual indicators** - Screen border and banner show recording/playing status
          - ⚙️ **Playback speed control** - Adjust from 0.1x to 10x speed
          - ⏱️ **Delay between loops** - Set custom delays between repetitions
          - ⌨️ Customizable hotkeys (F1, F2, F3, ESC by default)
          - 🔁 Loop control (0-100 times or infinite)
          - 📝 Real-time event log with timestamps
          - 🎮 Simple and intuitive GUI
          
          ### 🎯 Quick Start
          **Recording & Playback:**
          1. Press **F1** to start/stop recording
          2. Perform your clicks and key presses
          3. Set loop count (0 = infinite) and playback speed
          4. Press **F2** to play/stop recording
          5. Press **ESC** to force stop
          
          **Spam Click Mode:**
          - Press **F3** to toggle rapid-fire left clicking
          - Perfect for saving time with repetitive clicking
          
          ### 📚 Documentation
          See [README.md](https://github.com/${{ github.repository }}/blob/main/README.md) for full documentation
          
          ### ⚠️ Safety Notice
          Use responsibly! Ensure you have permission to automate any application.
          
          ---
          **System Requirements:** Windows 7/8/10/11
        files: |
          dist/AutoClicker.exe
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
